# iti-python-cu-labone

to remove: git branch -d localBranchName 
           git push origin --delete remoteBranchName


to list: git tag

deleting tag:  git tag -d v1.7  (local)
               git push origin --delete v1.7 (remote)

![Alt Text](git-github.png)
